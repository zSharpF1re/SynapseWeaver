# PostgreSQL (local Docker: docker compose up -d)
DATABASE_URL=postgresql://kguser:kgpass@localhost:5432/knowledge_graph

# Gemini (used from M2 onward; leave empty for M1)
GEMINI_API_KEY=

# Vercel Blob (required for file and inline image uploads)
BLOB_READ_WRITE_TOKEN=
